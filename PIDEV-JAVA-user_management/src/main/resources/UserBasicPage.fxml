<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.shape.Circle?>


<VBox fx:id="mainContainer" spacing="15" styleClass="light-theme" stylesheets="@style.css" xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1" fx:controller="gui.UserBasicPageController">
    <padding>
        <Insets bottom="25" left="25" right="25" top="25"/>
    </padding>

    <!-- Header with Welcome, Logout, and Theme Toggle -->
    <HBox alignment="CENTER_RIGHT" spacing="10">
        <VBox HBox.hgrow="ALWAYS">
            <Label styleClass="title-label" text="👤 Mon Profil" />
            <Label fx:id="welcomeLabel" text="" style="-fx-font-size: 14px; -fx-text-fill: #888; -fx-padding: 0 0 0 5;" />
        </VBox>
        <Button onAction="#logout" text="🚪 Déconnexion" styleClass="btn-danger" />
        <Button onAction="#toggleTheme" text="🌙 Mode Sombre" styleClass="theme-toggle-btn" />
    </HBox>

    <!-- Profile Card -->
    <VBox spacing="15" styleClass="input-card">
        <HBox alignment="CENTER_LEFT" spacing="10">
            <Label styleClass="subtitle-label" text="📋 Mes Informations" />
            <Region HBox.hgrow="ALWAYS" />
            <Label fx:id="roleBadge" styleClass="role-badge" />
        </HBox>

        <Separator />

        <!-- Profile Picture Section -->
        <HBox spacing="20" alignment="CENTER">
            <VBox alignment="CENTER" spacing="10">
                <Circle fx:id="profileCircle" radius="50" fill="#667eea" stroke="#ddd" strokeWidth="2" />
                <Button onAction="#uploadPhoto" text="📷 Changer la photo" styleClass="btn-info" />
            </VBox>
        </HBox>

        <Separator />

        <!-- Information Grid -->
        <GridPane hgap="15" vgap="12">
            <padding>
                <Insets top="5" bottom="5"/>
            </padding>

            <!-- Row 1: First Name -->
            <Label text="Prénom :" styleClass="label-bold" GridPane.columnIndex="0" GridPane.rowIndex="0" />
            <TextField fx:id="firstNameField" promptText="Prénom" GridPane.columnIndex="1" GridPane.rowIndex="0" />

            <Label text="Nom :" styleClass="label-bold" GridPane.columnIndex="2" GridPane.rowIndex="0" />
            <TextField fx:id="lastNameField" promptText="Nom" GridPane.columnIndex="3" GridPane.rowIndex="0" />

            <!-- Row 2: Age and Email -->
            <Label text="Âge :" styleClass="label-bold" GridPane.columnIndex="0" GridPane.rowIndex="1" />
            <TextField fx:id="ageField" promptText="Âge" GridPane.columnIndex="1" GridPane.rowIndex="1" />

            <Label text="Email :" styleClass="label-bold" GridPane.columnIndex="2" GridPane.rowIndex="1" />
            <TextField fx:id="emailField" promptText="Email" GridPane.columnIndex="3" GridPane.rowIndex="1" />

            <!-- Row 3: Role (Read Only) -->
            <Label text="Rôle :" styleClass="label-bold" GridPane.columnIndex="0" GridPane.rowIndex="2" />
            <TextField fx:id="roleField" editable="false" promptText="Rôle" GridPane.columnIndex="1" GridPane.rowIndex="2" />
        </GridPane>

        <Separator />

        <!-- Change Password Section -->
        <VBox spacing="10">
            <Label text="🔒 Changer le mot de passe" styleClass="subtitle-label" />

            <HBox spacing="15">
                <VBox spacing="5" HBox.hgrow="ALWAYS">
                    <Label text="Nouveau mot de passe" style="-fx-font-size: 12px;" />
                    <PasswordField fx:id="newPasswordField" promptText="Nouveau mot de passe" styleClass="password-field" />
                </VBox>
                <VBox spacing="5" HBox.hgrow="ALWAYS">
                    <Label text="Confirmer le mot de passe" style="-fx-font-size: 12px;" />
                    <PasswordField fx:id="confirmPasswordField" promptText="Confirmer le mot de passe" styleClass="password-field" />
                </VBox>
            </HBox>
            <Label text="Laissez vide pour garder le mot de passe actuel" style="-fx-font-size: 11px; -fx-text-fill: #888; -fx-padding: 0 0 0 5;" />
        </VBox>
    </VBox>

    <!-- Action Buttons -->
    <HBox spacing="20" alignment="CENTER">
        <Button onAction="#updateProfile" text="💾 Enregistrer les modifications" styleClass="btn-primary" />
        <Button onAction="#refreshProfile" text="🔄 Actualiser" styleClass="btn-info" />
    </HBox>

    <!-- Stats Section (Optional) -->
    <HBox spacing="15" alignment="CENTER">
        <VBox spacing="5" styleClass="stat-card">
            <Label text="📅" style="-fx-font-size: 24px;" />
            <Label text="Membre depuis" style="-fx-font-size: 11px; -fx-text-fill: #888;" />
            <Label fx:id="memberSinceLabel" text="2026" style="-fx-font-weight: bold; -fx-font-size: 14px;" />
        </VBox>
        <VBox spacing="5" styleClass="stat-card">
            <Label text="🆔" style="-fx-font-size: 24px;" />
            <Label text="ID Utilisateur" style="-fx-font-size: 11px; -fx-text-fill: #888;" />
            <Label fx:id="userIdLabel" text="-" style="-fx-font-weight: bold; -fx-font-size: 14px;" />
        </VBox>
    </HBox>
</VBox>