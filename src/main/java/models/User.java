package models;

/**
 * DEPRECATED: Please use entities.User instead.
 * This file is kept empty to prevent compilation conflicts with entities.User.
 */
public class User {
    // This class is empty to avoid "duplicate class" or "incompatible types" errors.
    // All properties and logic have been moved to entities.User.

    public int getId() { return 0; }
    public void setId(int id) {}
    // Stub methods to prevent immediate breaks if some code still references this class
}
